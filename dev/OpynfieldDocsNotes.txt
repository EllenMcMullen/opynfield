sphinx-apidoc -f -o source ../src/opynfield

run this every time i need to update the docs for opynfield
paths relative to docs folder

cd Documents/GitHub/opynfield/docs

delete contents of all under builds
then to regenerate the html run:

make html
